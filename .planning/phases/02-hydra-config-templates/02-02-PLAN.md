---
phase: 02-hydra-config-templates
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml
autonomous: true
requirements:
  - CFG-02

must_haves:
  truths:
    - "File `sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml` exists"
    - "File inherits `base.yaml` via `defaults: [/configs/custom_finetune/base, _self_]`"
    - "`scratch.lr_scale: 0.03` is explicitly set to document the decoder-only strategy"
    - "File contains only delta fields (≤ 10 lines of YAML fields excluding comments and header)"
  artifacts:
    - path: "sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml"
      provides: "Named decoder-only fine-tune strategy — inherits base.yaml, documents near-frozen backbone"
      contains: "/configs/custom_finetune/base"
  key_links:
    - from: "defaults list"
      to: "sam3/train/configs/custom_finetune/base.yaml"
      via: "Hydra defaults inheritance: /configs/custom_finetune/base"
      pattern: "/configs/custom_finetune/base"
---

<objective>
Create `sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml` — a minimal override config that inherits `base.yaml` and explicitly documents the decoder-only training strategy.

Purpose: Satisfies CFG-02. Allows users to launch decoder-only training with an explicitly named config (`-c custom_finetune/finetune_strategy/decoder_only`) rather than just the default. `base.yaml` already defaults to decoder-only — this file documents the strategy as a named, switchable option matching the full_finetune.yaml pattern.

Output:
- `sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml` — ~15-line YAML, inherits base.yaml
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-hydra-config-templates/02-CONTEXT.md
@.planning/phases/02-hydra-config-templates/02-01-SUMMARY.md

<interfaces>
<!-- Config inheritance pattern from sam3/train/configs/silver_image_evals/sam3_silver_image_yt1b.yaml -->
```yaml
# @package _global_
defaults:
  - /configs/eval_base.yaml   # ← absolute path from sam3/train/ root
  - _self_
```

<!-- Hydra config module root: initialize_config_module("sam3.train", version_base="1.2") -->
<!-- /configs/custom_finetune/base → sam3/train/configs/custom_finetune/base.yaml -->

<!-- Locked decisions from CONTEXT.md: -->
<!-- D-03: overlay contains ONLY LR/freeze strategy delta fields (2-3 fields each) -->
<!-- D-08: decoder_only delta = lr_scale: 0.03 -->
<!-- D-04: launch command = python sam3/train/train.py -c custom_finetune/finetune_strategy/decoder_only -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create finetune_strategy/decoder_only.yaml</name>
  <files>sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml</files>
  <read_first>
    - sam3/train/configs/custom_finetune/base.yaml (verify it exists from Plan 01; confirm scratch.lr_scale and lr_vision_backbone values)
    - .planning/phases/02-hydra-config-templates/02-CONTEXT.md (D-03: delta-only, D-08: lr_scale: 0.03)
    - .planning/phases/02-hydra-config-templates/02-01-SUMMARY.md (confirm base.yaml was created correctly)
  </read_first>
  <action>
Create directory `sam3/train/configs/custom_finetune/finetune_strategy/` if it does not exist. Write the following YAML content to `sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml`:

```yaml
# @package _global_
defaults:
  - /configs/custom_finetune/base  # inherit complete config from base.yaml
  - _self_                         # allow fields below to override inherited values

# ============================================================================
# Decoder-Only Fine-Tuning Strategy (D-08)
#
# This is the DEFAULT strategy — base.yaml already ships with these values.
# Use this config explicitly when you want a named, self-documenting run:
#
#   python sam3/train/train.py -c custom_finetune/finetune_strategy/decoder_only \
#     --use-cluster 0 --num-gpus 1
#
# Strategy: backbone is effectively near-frozen (lr_vision_backbone = 2.5e-6).
# Recommended for datasets < 500 images to avoid overfitting the ViT trunk.
# ============================================================================
scratch:
  lr_scale: 0.03  # decoder-only: near-freeze backbone (same as base.yaml default; explicit here for clarity)
```

This file contains only 1 effective override field (`lr_scale: 0.03`) — all other training configuration is inherited from `base.yaml` via the defaults list. The `lr_scale` value is the same as `base.yaml`'s default, making this a no-op override that serves as documentation of intent and a named launch target.
  </action>
  <acceptance_criteria>
    - `test -f sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml` exits 0
    - `grep "/configs/custom_finetune/base" sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml` returns a match
    - `grep "lr_scale: 0.03" sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml` returns a match
    - `grep "# @package _global_" sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml` returns a match
    - `python -c "import yaml; yaml.safe_load(open('sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml'))"` exits 0
    - File does NOT contain `enable_segmentation`, `train_norm_mean`, `trainer:`, or any field duplicated from base.yaml (it's a delta-only file)
  </acceptance_criteria>
  <done>
    `sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml` exists, inherits base.yaml via defaults list, contains `lr_scale: 0.03`, is valid YAML, and contains no duplicated base fields (delta-only per D-03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hydra defaults resolution | /configs/custom_finetune/base path must resolve within sam3.train config module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02a | Tampering | Incorrect defaults path breaks inheritance | mitigate | Acceptance criteria verifies exact path string `/configs/custom_finetune/base` is present |
</threat_model>

<verification>
```bash
# File exists and is valid YAML
test -f sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml && echo "EXISTS"
python -c "import yaml; yaml.safe_load(open('sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml')); print('VALID YAML')"

# Verify delta-only: no base fields duplicated
grep -E "enable_segmentation|train_norm_mean|trainer:|scratch:\s*$" sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml  # only scratch: section header is OK

# Verify inheritance
grep "/configs/custom_finetune/base" sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml  # → match
grep "lr_scale: 0.03" sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml  # → match
```
</verification>

<success_criteria>
- `sam3/train/configs/custom_finetune/finetune_strategy/decoder_only.yaml` exists with valid YAML
- Inherits base.yaml via `defaults: [/configs/custom_finetune/base, _self_]`
- Contains only `scratch.lr_scale: 0.03` as the effective field override (delta-only per D-03)
- No duplication of base.yaml fields
</success_criteria>

<output>
After completion, create `.planning/phases/02-hydra-config-templates/02-02-SUMMARY.md`
</output>
